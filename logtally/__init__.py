"""logtally — structured log triage CLI.

Streams logs, matches lines against configurable regex patterns, and
produces summary statistics. Memory-efficient: handles files larger
than RAM.
"""

__version__ = "0.1.0"
